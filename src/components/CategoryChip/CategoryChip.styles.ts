import { StyleSheet } from 'react-native'

export const styles = StyleSheet.create({
  chip: {
    paddingHorizontal: 20,
    paddingVertical: 10,
    borderRadius: 24,
    borderWidth: 1.5,
  },
  chipText: {
    fontSize: 15,
    letterSpacing: 0.3,
  },
})
